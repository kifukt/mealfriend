<%= form_with(model: intake, local: true) do |form| %>
  <% if intake.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(intake.errors.count, "error") %> prohibited this intake from being saved:</h2>

      <ul>
      <% intake.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="input-field">
    <%= form.label :user_id %>
    <%= form.collection_select(:user_id, User.all, :id, :login) %>
  </div>
  

  <div class="input-field">
    <%= form.label :ingredient_id %>
    <%= form.collection_select(:ingredient_id, Ingredient.all, :id, :name) %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount, id: :intake_amount %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
