<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :login %>
    <%= form.text_field :login, id: :user_login %>
  </div>

  <div class="field">
    <%= form.label :birthdate %>
    <%= form.date_select :birthdate, id: :user_birthdate %>
  </div>

  <div class="field">
    <%= form.label :height %>
    <%= form.number_field :height, id: :user_height %>
  </div>

  <div class="field">
    <%= form.label :sex %>
    <%= form.check_box :sex, id: :user_sex %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
